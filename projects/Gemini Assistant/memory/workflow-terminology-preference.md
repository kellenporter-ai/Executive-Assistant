---
name: workflow-terminology-preference
description: Use Backward Design → Delegate → Build → QA → Deploy for workflows.
type: feedback
---

Always use the specific workflow terminology: **Backward Design → Delegate → Build → QA → Deploy**.

**Why:** The user specifically corrected the dev pipeline to align with this pedagogical strategy and the Redesign Loop concept (returning to Backward Design if QA fails).
**How to apply:** When referencing phases of work, planning tasks, or executing the dev pipeline, use these exact phase names rather than generic terms like "Design" or "Execute".
